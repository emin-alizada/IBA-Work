const defaults = {
    speed: 500,
    loop: false,
    grabCursor: true,
    slidesPerView: 'auto',
    autoHeight: true,
    autoplay: {
        delay: 5000
    }
};

const slider =  new Swiper(".swiper", defaults);